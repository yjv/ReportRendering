{% block javascripts %}
{% javascripts
    '@YjvReportRenderingBundle/Resources/public/js/html_report.js'
%}
<script type="text/javascript" src="{{ asset_url }}"></script>
{% endjavascripts %}
{% endblock %}
<div{{ widget.attributes|attributes({"id": widget.reportId, "class": "report"}) }}>
{% if widget.hasFilterForm %}
{% block report_filters %}
<div id="report-filters{{ widget.reportId }}" class="report-filters well well-small collapse">
	<div>
		<button type="button" data-toggle="collapse" data-target="#report-filters{{ widget.reportId }}" class="btn" style="">Filters</button>
	</div>
	<div>
		<form>
			{% for filter in widget.filterForm.createView() %}
			{{ form_label(filter) }}
			{{ form_widget(filter) }}
			{% endfor %}
		</form>
	</div>
</div>
{% endblock %}
{% endif %}
{% block report_data %}
<table class="report-data table table-striped table-bordered table-hover">
	{% block report_header %}
	<thead>
		{% block report_header_row %}
		<tr>
			{% for column in widget.columns %}
				{% block report_header_cell %}
				{% set attributes = column.attributes %}
				<th {{ attributes.attr|default({})|attributes }}>
					{% if attributes.sortable|default(false) %}
						<a href="javascript:void(0)">{{ attributes.name|default('') }}</a>
					{% else %}
						{{ attributes.name|default('') }}
					{% endif %}
				</th>
				{% endblock %}
			{% endfor %}
		</tr>
		{% endblock %}
	</thead>
	{% endblock %}
	{% block report_body %}
	<tbody>
		{% for row in widget %}
			{% block report_row %}
			<tr {{ row.attributes|attributes }}>
				{% for cell in row.cells %}
					{% block report_cell %}
					<td {{ cell.attributes|attributes }}>{{ cell.data|raw }}</td>
					{% endblock %}
				{% endfor %}
			</tr>
			{% endblock %}
		{% endfor %}
	</tbody>
	{% endblock %}
</table>
{% endblock %}
</div>